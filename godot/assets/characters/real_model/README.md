# Real Model Drop Folder

Place the first real character GLB here:

```text
godot/assets/characters/real_model/body.glb
```

The V0.3 runtime only prepares loading and socket mapping. It does not implement animation retargeting, IK, physics, or bone-level control.

